
basket->items->add(item) // OK
             ->has(item) // OK
             ->remove(item, qty)
             ->update(item, closure) // OK
             ->item->update(data)
                   ->call(closure)

basket->payments->add(payment)
                ->has(payment)
                ->remove(payment, amount)
                ->payment->update(data)
                         ->call(closure)

basket->summaries->balance()
                 ->vat()
                 ->deals()

basket->empty()
